apiVersion: v1
kind: Service
metadata:
  name: {{ include "ars.fullname" . }}
  labels:
    {{- include "ars.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ars.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
