from django.urls import path, include
from . import api

apipatterns = [
    path(r'', api.index, name='ara-YOUR-api'),
    path(r'runYOURquery', api.runquery, name='ara-YOUR-runquery')
]

urlpatterns = [
    path(r'api/', include(apipatterns)),
]
